{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

{/* ------------------------------------------------------------------------ */}

### Update `CHANGELOG.md`

- Open the `CHANGELOG.md` file
- Check if all previously fixed issues are included
- Check the latest commits `npm run git-log`; if necessary, copy/paste lines,
  group by dates and edit them using the regular expressions below
- To convert the dates into headings, change from:
  ```txt


  ([0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]) [*]
  ```
  to:
  ```txt


  ## $1

  *
  ```
- To remove the remaining dates, change from:
  ```txt
  ^[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9] [*]
  ```
  to:
  ```txt
  *
  ```
- Add a new entry such as _**\* v14.2.1-1.1 prepared**_
- **Commit** with the message _**prepare v14.2.1-1.1**_
